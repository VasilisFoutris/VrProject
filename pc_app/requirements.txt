# VR Screen Streamer - PC Application Dependencies
# Install with: pip install -r requirements.txt

# GUI Framework
PyQt5>=5.15.0

# Screen Capture
mss>=9.0.0
Pillow>=10.0.0

# Video Encoding/Compression
numpy>=1.24.0
opencv-python>=4.8.0

# Fast JPEG encoding (optional but recommended - 3-5x faster)
# Requires libturbojpeg to be installed on system
# Windows: Download from https://github.com/libjpeg-turbo/libjpeg-turbo/releases
PyTurboJPEG>=1.7.0

# WebSocket Server for streaming
websockets>=12.0
aiohttp>=3.9.0

# Network utilities
netifaces>=0.11.0

# Windows-specific for window enumeration
pywin32>=306

# Performance monitoring
psutil>=5.9.0

# Configuration
pyyaml>=6.0.0

# QR Code generation
qrcode[pil]>=7.4.0

# ============================================
# GPU ACCELERATION (Optional but recommended)
# ============================================
# These packages enable GPU-accelerated encoding for maximum FPS/minimum latency.
# They require NVIDIA GPU with CUDA support.

# CuPy for GPU image processing (choose your CUDA version):
# For CUDA 11.x: pip install cupy-cuda11x
# For CUDA 12.x: pip install cupy-cuda12x
cupy-cuda12x>=13.0.0

# nvJPEG for hardware JPEG encoding (requires NVIDIA GPU):
# pip install pynvjpeg
pynvjpeg>=0.1.0

# OpenCV with CUDA support (optional, requires building from source):
# opencv-contrib-python does NOT include CUDA - you need to build OpenCV with CUDA manually
# See: https://docs.opencv.org/master/d6/d15/tutorial_building_tegra_cuda.html
